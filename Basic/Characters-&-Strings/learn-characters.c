// Basic character

#include <stdio.h>
#include <ctype.h> //Header file to access functions of characters
int main()
{
    // Declare,input and take a Character
    char character1 = 'A';
    char character2;
    scanf("%c", &character2);
    printf("character is = %c\n", character2);
    printf("ASCII Value of the character 1 = %d, \n", character1);

    // islower function checks whether a character is lowercase alphabet or not

    // char lowerCase = 'N';
    // int islower(lowerCase);
}
